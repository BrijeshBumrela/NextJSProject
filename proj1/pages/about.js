import Layout from '../components/Layout.js';

const About = (props) => (
    <React.Fragment>
        <Layout >
            This is the about page 
        </Layout>    
    </React.Fragment>
)

export default About;