import Layout from '../components/Layout.js';
import Head from 'next/head';

const HireMe = (props) => (
    <React.Fragment>
        <Head>
            <title>Hire Me</title>
            <meta name="HireMe Page" description="Hire Me Page"></meta>
        </Head>
        <Layout >
            Contact here
        </Layout>    
    </React.Fragment>
)

export default HireMe;