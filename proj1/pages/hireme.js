import Layout from '../components/Layout.js';

const HireMe = (props) => (
    <React.Fragment>
        <Layout >
            Contact here
        </Layout>    
    </React.Fragment>
)

export default HireMe;